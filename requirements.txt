customtkinter==5.2.2
mammoth==1.11.0
openpyxl==3.1.5
# openpyxl reads an image through Pillow; without it a picture
# cannot be embedded in a workbook and only its path is kept.
pillow==12.3.0

# PDF: pdfplumber (on pdfminer.six) reads the text and the tables out of a
# page, pypdf pulls the embedded images. Writing a PDF is Word's or
# LibreOffice's job, so nothing here is needed for that direction.
pdfplumber==0.11.10
pypdf==6.16.2
python-docx==1.2.0
tabulate==0.9.0
tkinterdnd2==0.4.3

# pandas publishes win_arm64 wheels only from 3.0 onwards, so the ARM64 build
# needs a newer pin than the x64 one. Both produce identical Markdown and the
# whole test suite passes on either version.
pandas==2.3.3; platform_machine != "ARM64"
pandas>=3.0.5; platform_machine == "ARM64"

# Legacy Office 97-2003 formats
xlrd==2.0.2
olefile==0.47
pywin32==312; sys_platform == "win32"
